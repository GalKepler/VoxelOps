version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  commands:
    - pip install --upgrade pip
    - pip install -e .[docs]

sphinx:
  configuration: docs/conf.py
  html_dir: _build/html
  
formats:
  - html
